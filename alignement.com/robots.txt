# =====================================================
# Alignement.com — robots.txt
# =====================================================
#
# NOTE : robots.txt contrôle l'EXPLORATION, pas l'indexation.
# Un « Disallow » n'empêche pas Google d'indexer une URL —
# il l'empêche seulement de la lire, donc de voir un noindex.
# Ce qui ne doit pas être indexé est marqué « X-Robots-Tag:
# noindex » dans le .htaccess, pas bloqué ici.

# Tous les robots
User-agent: *
Allow: /

# Sitemap
Sitemap: https://alignement.com/sitemap.xml
